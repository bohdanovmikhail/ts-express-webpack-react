// Global imports
import { Subject } from 'rxjs';

// Utils, helpers, etc. imports

// Constants


// Run code
export class Bus {
  private _events$: Subject<void> = new Subject();
}

// Modules, interfaces, types declarations
